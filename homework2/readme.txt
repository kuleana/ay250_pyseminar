# Katherine de Kleer
# Homework #2 (AY 250) README

please run as follows:
> run getdist

followed by:

if you want to simply create a distribution [stores values but does not print or plot anything]:
> m, samples, ratio = sampler('(1.0/2.0)*np.exp(-abs(x))',stats.cauchy,1000)
(for example)

the following create plots & print statements that answer the questions:
> getdist('b')
> getdist('c')
> getdist('d')
